__pycache__/
*.py[cod]
.DS_Store
*.egg-info/
.venv/
.pytest_cache/
/assets/
app/build/dist/
app/build/work/
app/desktop/dist/
app/desktop/node_modules/
app/desktop/src-tauri/target/
app/desktop/src-tauri/binaries/sprite-mcp-*
.gjc/
uv.lock

# transient validator reply scratch (never track)
.validator-reply-*

# 에이전트 작업 지침 — 로컬 전용 (운영 맥락이라 공개 레포에 두지 않는다)
CLAUDE.md

# local kuma tooling / wrangler cache (never track)
.kuma/
.wrangler/

# build artifacts the release workflow produces
/build/
/dist/

# local pipeline demos / smoke outputs (regenerated; ~19MB of PNGs)
/runs/
/smoke-out/
smoke.billed.toml
smoke.cli.toml
smoke.providers.toml
*.sg-rwlock
